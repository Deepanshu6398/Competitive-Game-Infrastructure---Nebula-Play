{
  "name": "fault-tolerant-data-processor",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency prototype for unreliable event ingestion, normalization, idempotency, and aggregation.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  }
}
